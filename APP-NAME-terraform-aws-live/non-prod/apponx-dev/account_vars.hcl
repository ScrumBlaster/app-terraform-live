#-----------------------------------------------------------------------------------------------------------------------
# Common inputs for development stage environments
inputs = {
  #---------------------------------------------------------------------------------------------------------------------
  aws_account_id = "xxxxxxxxxx"      // apponx-dev - development account (Appon)
  aws_iam_role   = "abc_name" // IAM role used to deploy resources. Currently one IAM role for each account (stage)

}